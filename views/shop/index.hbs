{{# each products}}
    <div class="row">
        {{# each this }}
            <div class="col-sm-6 col-md-4">

                <div class="card">
                    <div class="card-header">
                       Feature Item     
                    </div>
                    <div class="card-body">
                         <img src="{{ this.imagePath }}"
                            alt="..."
                            class="img-responsive">
                        <h5 class="card-title">{{ this.title }}</h5>
                        <p class="card-text">{{ this.description }}</p>
                        <p class="card-text">
                            <strong>${{ this.price }}</strong>
                        </p>
                        <a href="/add-to-cart/{{ this.id }}" class="btn btn-primary" role="button">Add to Shopping Cart</a>
                    </div>
                </div>
                {{!-- <div class="thumbnail">
                    <img src="{{ this.imagePath }}"
                         alt="..."
                         class="img-responsive">
                    <div class="caption">
                        <h3>{{ this.title }}</h3>
                        <p>{{ this.description }}</p>
                        <div class="clearfix">
                            <div> 
                                 <strong>${{ this.price }}</strong>
                                 </div>
                            <a href="/add-to-cart/{{ this.id }}" class="btn btn-success" role="button">Add to Shopping Cart</a>
                        </div>
                    </div>
                </div> --}}
            </div>
        {{/each}}
    </div>
{{/each}}
